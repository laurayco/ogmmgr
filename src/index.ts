import { ModeEntry, ModeIndex, AuthorInfo } from "./models";

/*
    this file is the entry point to build the website.
*/

async function get_game_mode_entries()
: Promise<ModeEntry[]> {
    return [];
}